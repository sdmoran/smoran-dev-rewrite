<!DOCTYPE html>
<html>
    <head>
        <title>smoran.dev</title>
        <%- include('../partials/head.ejs') %>
    </head>
    <body>
        <div class="flex flex-col  justify-center space-y-20 ">
            <div class="m-16 flex flex-col items-center">
                <h1 class="text-blue-400">Welcome to my site!</h1>
                <p>I'm Sam. I'm a senior studying Computer Science at Worcester Polytechnic Institute.</p>
            </div>
            <!-- Projects -->
            <div class="flex flex-col items-center space-y-6">
                <h1 class="text-blue-400">Projects</h1>

                <div class="flex flex-row items-start space-x-20 ">
                    
                    <% for(let i = 0; i < 3; i++) { %>
                        <% project = projects[i] %>
                        <div class="w-48 h-16 items-center text-center rounded-md bg-blue-300 p-4" onclick="location.href = '/projects/<%= project.name %>'"><%= project.name %></div>
                    <% }%>
                </div>
            </div>
        </div>
    </body>
</html>
