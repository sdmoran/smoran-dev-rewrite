const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    colors: {
      amber: colors.amber,
    }
  },
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
}
